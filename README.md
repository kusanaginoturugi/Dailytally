# Dailytally

「毎日集計」用のシンプルなWebアプリです。

## できること

- 9つの伝道会ごとに、1週間分（7日）の数字を入力
- 7〜9項目を切り替えて入力（既定は9項目）
- 合計ページで、9伝道会分の日次合計と週間合計を確認
- 週の開始日を変更して運用可能
- 先頭の管理ページで週の開始日・項目数・得道者数開始日・護摩供回数を確認
- 得道者数開始日を指定して、合計ページの見出しに反映
- 護摩供回数は週の開始日の年から自動計算（2026年は31回、2027年は32回）
- 目標数は週の初日のみ入力可能
- 入力内容はCloudflare D1に自動保存され、全員で同じデータを確認可能
- 以前のブラウザ保存データがある場合、DBが空なら初回表示時にD1へ移行
- 最後に開いていたタブ（各伝道会/合計ページ）も保存して復元

## 使い方

1. Cloudflare WorkersにデプロイしたURLをブラウザで開く
2. 「管理ページ」タブで「週の開始日」「項目数（7/8/9）」「得道者数開始日」を設定
3. 上部タブから各伝道会名を開いて入力
4. 「合計ページ」タブで全伝道会合計を確認

## Cloudflare D1設定

1. 依存関係をインストール
   `npm install`
2. D1データベースを作成
   `npx wrangler d1 create dailytally`
3. 表示された `database_id` を `wrangler.toml` の `database_id` に設定
4. D1マイグレーションを適用
   `npm run db:migrate:remote`
5. Workersへデプロイ
   `npm run deploy`
